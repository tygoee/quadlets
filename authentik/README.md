# Additional configuration

The systemd service to start is `authentik-server`, the rest follows automatically. Visit port 9000 to access it. When forwarding it with a reverse proxy make sure to have websockets enabled.

# Sources

<https://hub.docker.com/r/authentik/server>

<https://docs.goauthentik.io/install-config/install/docker-compose/>
